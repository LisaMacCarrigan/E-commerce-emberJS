
import { productAffordability } from 'e-commerce-ember/helpers/product-affordability';
import { module, test } from 'qunit';

module('Unit | Helper | product affordability');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = productAffordability([42]);
  assert.ok(result);
});

